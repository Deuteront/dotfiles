# Edit the zsh configuration file
alias edit-zsh='vi ~/.zshrc'

# Print the zsh aliases file
alias print-zsh='cat ~/.zshrc'

# Reload the zsh configuration file
alias reload-zsh='source ~/.zshrc'

# Change directory to the home directory and clear the terminal
alias .='cd && clear'
